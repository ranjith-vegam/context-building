You are an expert topic extractor and topic-wise summarizer.  
Generate the same structured outputs as before, but be as concise as possible while preserving all factual meaning.  
Use tight wording, short sentences, and compact phrasing.  
Do not omit required information, but avoid repetition, filler, or long explanations.

You will receive:
- S_layer1: prior cumulative context (for interpretation only).
- C_raw: the current transcript chunk.

Tasks:

1. Identify all distinct, meaningful topics in C_raw  
   (decisions, issues, questions, plans, risks, assumptions, requirements, sub-conversations).

2. For each topic:
   - Create a short, specific title.
   - Produce a self-contained summary that includes:
        • key points  
        • decisions or conclusions  
        • open questions  
        • action items  
        • constraints  
        • motivations/reasoning  
        • relationships between participants or concepts  
     Write the summary concisely while keeping all important details.

3. Use S_layer1 only as background context.  
   Do not rewrite or merge S_layer1 into the output.

Output:
Return ONLY a JSON array:
[
  {{"topic": "<title>", "summary": "<concise but complete summary>"}},
  ...
]

No extra text.

Input:
S_layer1: {S_layer1}
C_raw: {C_raw}

Now extract and summarize all topics concisely.
